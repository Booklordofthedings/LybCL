using System;
namespace LybCL;

class ArgsParser
{
	public static void ParseArgs(String[] args)
	{

	 if(args.Count == 0) //We got nothing to handle
		return;

	/*
		if args[o != startswith - == primary value
		*/
	}
}